# api-doc-lookup

快速查询 API 和库的用法，不离开编辑器。支持 TypeScript 和 Python 生态。

## 安装

**Cursor：** 将此文件夹放到 `~/.cursor/skills-cursor/` 或项目的 `.cursor/skills/` 下。

**Claude Code：** 将此文件夹放到项目的 `.claude/skills/` 下。

## 使用方式

> "prisma 的 upsert 怎么用"
> "FastAPI 文件上传"
> "useEffect 的依赖数组怎么写"

## 依赖

无特殊依赖。
