# sql-assistant

自然语言转 SQL，SQL 优化与解释，支持 Prisma 和 SQLAlchemy ORM。

## 安装

**Cursor：** 将此文件夹放到 `~/.cursor/skills-cursor/` 或项目的 `.cursor/skills/` 下。

**Claude Code：** 将此文件夹放到项目的 `.claude/skills/` 下。

## 使用方式

> "查出最近 7 天注册的活跃用户"
> "这段 SQL 帮我优化一下"
> "prisma 怎么写多表 join"

## 依赖

无特殊依赖。
