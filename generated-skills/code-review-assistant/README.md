# code-review-assistant

自动化代码审查助手，检查安全漏洞、性能问题、类型安全和代码规范。支持 TypeScript 和 Python。

## 安装

**Cursor：** 将此文件夹放到 `~/.cursor/skills-cursor/` 或项目的 `.cursor/skills/` 下。

**Claude Code：** 将此文件夹放到项目的 `.claude/skills/` 下。

## 使用方式

> "帮我审查一下 src/auth.ts"
> "review 最近的改动"
> "这段代码有什么问题"

## 依赖

无特殊依赖。
