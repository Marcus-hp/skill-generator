# doc-generator

根据代码自动生成 README、API 文档、CHANGELOG 和代码注释。

## 安装

**Cursor：** 将此文件夹放到 `~/.cursor/skills-cursor/` 或项目的 `.cursor/skills/` 下。

**Claude Code：** 将此文件夹放到项目的 `.claude/skills/` 下。

## 使用方式

> "帮我写个 README"
> "生成 API 文档"
> "给这些函数加上 JSDoc"

## 依赖

无特殊依赖。
