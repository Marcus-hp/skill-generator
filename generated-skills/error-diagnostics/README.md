# error-diagnostics

自动解析报错信息，定位根因，提供修复方案。支持 TypeScript/Node.js 和 Python 错误。

## 安装

**Cursor：** 将此文件夹放到 `~/.cursor/skills-cursor/` 或项目的 `.cursor/skills/` 下。

**Claude Code：** 将此文件夹放到项目的 `.claude/skills/` 下。

## 使用方式

> 直接粘贴报错信息
> "运行 npm run dev 报错了"
> "这个 TypeError 怎么修"

## 依赖

无特殊依赖。
