# refactor-helper

安全重构代码，自动分析影响范围。支持提取、拆分、重命名、消除重复等操作。

## 安装

**Cursor：** 将此文件夹放到 `~/.cursor/skills-cursor/` 或项目的 `.cursor/skills/` 下。

**Claude Code：** 将此文件夹放到项目的 `.claude/skills/` 下。

## 使用方式

> "帮我把这个函数提取出来"
> "这个文件拆分一下"
> "所有 fetch 换成 axios"

## 依赖

无特殊依赖。
