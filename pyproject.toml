[project]
name = "skill-generator"
version = "2.0.0"
description = "交互式 Agent Skill 生成向导"
requires-python = ">=3.10"
dependencies = [
    "jsonschema",
]

[project.optional-dependencies]
dev = [
    "pytest",
    "pytest-cov",
]
